01. how to clear console in C

    #include <stdlib.h>

    system("cls");

02. how to get string in this project

    char *str = getString("Enter a string");
    printf("You entered : %s\n", str);

03. how to get integer in this project

    int integer = getInteger("Enter a number");
    printf("Your entered : %d\n", integer);

04. how to get choice in this project

    char* options[] = { // array of options
        "yes",
        "no",
    };

    int numOptions = 2;  // number of options

    char* choice = getChoice(options, numOptions, "Choose an option");
    printf("You selected: %s\n", choice);
